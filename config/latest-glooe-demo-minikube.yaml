# This config requires the following environment variables or global valet config:
#  LICENSE_KEY
#  AWS_ACCESS_KEY_ID
#  AWS_SECRET_ACCESS_KEY
#  GITHUB_TOKEN

cluster:
  type: minikube
gloo:
  enterprise: true
  aws:
    secret: true
    upstream: true
demos:
  petclinic: {}